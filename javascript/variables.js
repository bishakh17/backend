//let variables can be changed and do not need to be initialized
let age = 21;
let result;

//all const variables must be initialized and cannot be changed
const salary = 1000;

console.log(age);
console.log(salary);
console.log(result);